spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/eduforge}
    username: ${DB_USER:eduforge}
    password: ${DB_PASSWORD:eduforge}
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
